# 💉 InkMaster Tattoo Studio (Проект на базе Node.js, Express, Sequelize и MySQL)

Это веб-приложение представляет собой каталог эскизов (товаров/услуг) тату-студии. Имеет авторизацию, полный CRUD-интерфейс для админа и REST API, документированный через Swagger.

## ✨ Основной функционал

* **Аутентификация:** Вход/выход (сессии).
* **Роли:** `admin` (может добавлять, редактировать, удалять) и `user` (только просмотр).
* **Каталог:** Просмотр списка эскизов с ценами и стилем татуировки.
* **Админка:** Формы для создания/редактирования/удаления эскизов.
* **REST API:** Полный API для работы с эскизами (`/api/products`).
* **Документация API:** Доступна по адресу `/api-docs` (Swagger UI).

## 🛠️ Необходимые компоненты

Для работы приложения вам потребуется:

1.  **Node.js** (версия 18+).
2.  **MySQL Server** (рекомендуется использовать XAMPP, WAMP или Docker).

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
npm install