const productApiController = require('../../controllers/productApiController');

describe('productApiController', () => {
  it('should be defined', () => {
    expect(productApiController).toBeDefined();
  });

  // TODO: Add tests for list, getOne, create, update, remove methods
  // Remember to mock req, res, next objects and the Product model
});
